from langgraph.graph import StateGraph, END

from initstate import GraphState
# from nodes.classify import 

def create_collage_graph_flow():
    graph = StateGraph(GraphState)

    graph.add_node('classify', )
    graph.add_node('', )
    graph.add_node('', )
    graph.add_node('', )

def create_interview_graph_flow():
    pass